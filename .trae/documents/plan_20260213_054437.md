I will clone the design of `https://penalangit.id/` using Laravel Blade and Tailwind CSS.

### Implementation Plan

1.  **Create Main Layout** (`resources/views/layouts/app.blade.php`)
    *   Set up the HTML structure.
    *   Include the Tailwind CSS and Vite scripts (`@vite(['resources/css/app.css', 'resources/js/app.js'])`).
    *   Define the structure for Navbar, Content, and Footer.

2.  **Create Navbar and Footer**
    *   **Navbar**: Implement the menu items: "Home", "Ruang Tulisan", "Layanan", "Toko", "Kontak", "Profil".
    *   **Footer**: Add copyright info, social links, and contact details consistent with the reference.

3.  **Create Home Page View** (`resources/views/home.blade.php`)
    *   **Hero Section**: "Mau Menerbitkan Buku! Kami bantu anda..." with a call-to-action button.
    *   **Features Section**: 3 columns for "Setiap Buku Ber-ISBN", "Kualitas Terjamin", "Biaya Terjangkau".
    *   **Services Section**: Highlight "Penerbitan Buku" and "Toko Buku".
    *   **About Section**: A brief profile section with the CEO's quote.
    *   Use placeholder images (e.g., from `placehold.co`) where actual images are missing.

4.  **Update Routes** (`routes/web.php`)
    *   Change the default route (`/`) to return the new `home` view instead of `welcome`.

5.  **Clean Up**
    *   Ensure the design is responsive and uses the project's existing Tailwind v4 setup.
